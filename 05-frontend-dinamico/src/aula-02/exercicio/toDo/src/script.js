const form = document.forms['todo-form']

const tasks = []

form.addEventListener('submit', () => {
  const task = document.createElement() form.elements["task-name"].value
  tasks.push()

})


tasks.forEach(task => {
  body.appendChild()
});

